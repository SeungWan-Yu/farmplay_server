const farmModel = require('./farm.model');
const farmplerModel = require('./farmpler.model');
const recruitModel = require('./recruit.model');
const reviewModel = require('./review.model');
const userModel = require('./user.model');
const webViewModel = require('./webView.model');
const tourModel = require('./tour.model');

module.exports = {
  farmModel,
  farmplerModel,
  recruitModel,
  reviewModel,
  userModel,
  webViewModel,
  tourModel,
}